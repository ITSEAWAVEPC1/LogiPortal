# Seawave Freight Forwarding Platform

Next.js 16 (App Router) + TypeScript + Prisma 7 + Neon Postgres + NextAuth v5 + Tailwind v4.
Targets Vercel for hosting (not deployed yet — local dev only so far). See docs/platform-development-plan.md for full spec, roles, and staged build plan.
See docs/original-process-reference.pdf for the source field list per workflow step (page 1 = Organization Creation, page 2 = Enquiry Capturing, pages 3-6 = Import/Export Freight Forwarding process detail).

## Status

Stages 0, 1, 2 complete, committed, and pushed to `origin/main`. Customer Master v2 (Organization enhancement — Branches/Account Info/Billing/Bank Details/Customize Columns, built on Stage 1's Organization model), Stage 3 (Quotation Module), Stage 4 (Consignment / Job Creation + Historical Job Import), Stage 5 (Import Workflow Engine — Ex-Works & FOB tracks), Stage 6 (Export Workflow Engine — CIF/DDP/DDU tracks + Dock/Factory Stuffing), Stage 7 (Document Generation & Management — HBL/MBL/Freight Certificate/DO/Invoice), and Stage 9 (Customer Portal — read-only org-scoped `(portal)` route group, feature-flagged) also complete, not yet committed — see `docs/stage-checklists/customer-master-v2.md`, `docs/stage-checklists/stage-3.md`, `docs/stage-checklists/stage-4.md`, `docs/stage-checklists/stage-5.md`, `docs/stage-checklists/stage-6.md`, `docs/stage-checklists/stage-7.md`, `docs/stage-checklists/stage-9.md`. **Stage 8 (Real Integrations) was deliberately SKIPPED.**

**Stage 10 (Reporting, Notifications & Polish) complete**, split into four sub-stages, each on its own branch off the previous (`stage-10a-shadcn-dashboard` → `…-10b-reports-cxo` → `…-10c-notifications` → `…-10d-audit-hardening`), committed, **not yet merged or pushed** — see `docs/stage-checklists/stage-10a.md`…`stage-10d.md`. **10a**: shadcn/ui stack added alongside the existing 13 `src/components/ui/` primitives (they coexist — new surfaces use `src/components/shadcn/*`, old screens untouched); `cn` → `twMerge(clsx())`; `src/styles/shadcn-tokens.css` maps shadcn names to the brand palette; `next-themes` `forcedTheme="light"`; `src/lib/permissions/scope.ts` (4th permission mechanism — `dashboardScope`/`reportScope` row-scoping); new `/dashboard` (`src/lib/dashboard/`, `src/components/dashboard/`). **10b**: additive `Job.expectedDeliveryDate`/`actualDeliveryDate` + `@@index([branchId,createdAt])`; `src/lib/workflow/delivery-dates.ts` write-through in the workflow step route; four reports (`src/lib/reports/*` behind `runReport()`), `/reports` + `/reports/[report]`, `GET /api/reports/[report]/export` (CSV); on-time donut + CXO KPI band on `/dashboard` (`GET /api/dashboard/kpis`, `GET /api/dashboard/report.pdf` via new `src/lib/pdf/dashboard/`); committed guarded demo generator `prisma/seed-demo.ts` (`npm run db:seed:demo`). **10c**: `Notification` + `NotificationPreference` + enums `NotificationType`/`NotificationDeliveryStatus`; `src/lib/notifications/` (events → enqueue → dispatch with a `FOR UPDATE SKIP LOCKED` email-queue drain → `fire.ts` `fireAfterResponse()` wrapping Next 16 `after()`, always post-`$transaction`); `mail/` transport seam (log-only until `NOTIFICATIONS_EMAIL_ENABLED` + `RESEND_API_KEY`); `GET/PATCH/POST /api/notifications*`, `GET/PUT /api/notifications/preferences`, `GET /api/cron/notifications` (`CRON_SECRET`); `vercel.json` cron; `NotificationBell` in the Topbar + portal, `/notifications`, real `/settings/notifications` form. **10d**: `access-matrix.ts` `"audit"` screen (ADMIN + BRANCH_MANAGER); `src/lib/audit/` + `/audit` (job/portal/login tabs, branch-scoped via `reportScope`) + `GET /api/audit/export`; `Session.ipAddress`/`userAgent` now captured in `auth.ts`; `next.config.ts` security headers (CSP Report-Only); deleted `src/app/api/test/job-fields/route.ts`; additive indexes migration `20260903200000_stage_10d_audit_indexes`. **Stage 11 (Excel/Sheets cutover) is an operational process, not a code build.**

**Stage 12 (stakeholder change-request batch against Enquiry/Quotation) complete**, split into four sub-stages per the approved plan (12a → 12b → 12c → 12d), **not committed**. **12a complete** — see `docs/stage-checklists/stage-12a.md`: `EnquiryCommodityLine` (multi HS-code/commodity), `EnquiryFreightPackage` (repeatable LCL/FCL package rows) + `Port` master (`/settings/ports`), additive `finalDestinationAddress`/`portOfLoadingId`/`portOfDischargeId` on `EnquiryFreightDetail`, additive `length`/`width`/`height`/`dimensionUnit` (enum `DimensionUnit`) on `EnquiryTransportDetail`; dimensions/weight no longer mandatory. **12b complete** — see `docs/stage-checklists/stage-12b.md`: the Enquiry approval gate is removed (`submit` now goes straight `DRAFT/NEEDS_CORRECTION → READY_FOR_QUOTATION`; `review/route.ts` + the `"approve"` capability are dead code, left in place); editability is now Quotation-attachment-based, not status-based (`src/lib/enquiries/persist-draft.ts` shared by the PATCH and submit routes); `EnquiryList.tsx` gained an inline edit icon (`lucide-react`); `sonner` toasts used for the first time; submit is now a single combined persist+validate+transition call instead of two round trips. **12c complete** — see `docs/stage-checklists/stage-12c.md`: `EnquiryFieldConfig` (admin-configurable per-`(serviceType,fieldKey)` visibility/requiredness, `/settings/enquiry-fields`); `src/lib/enquiries/field-config-keys.ts` (client-safe canonical field list + checker) vs. `field-config.ts` (DB-backed, re-exports the former) — a real Prisma-in-client-bundle mistake was caught here by running `next build`, not just `tsc`; `enquirySubmitSchema` now only does structural/ODC checks, per-field requiredness moved to `checkConfigurableFieldRequirements()` called from the submit route. **12d complete** — see `docs/stage-checklists/stage-12d.md`: `createQuotationSchema` now `{organizationId, enquiryId}` (single-select radio, new `src/components/ui/Radio.tsx`); `QuotationLineItem` gained `remarks`/`exchangeRate`/`rateInr` — currency is genuinely per-line now (each row picks its own, default INR, auto-computed `rateInr`/`amount` with manual override); `QuotationVersion.currency` is pinned to `"INR"` unconditionally (the grand Total is always one INR figure); `LineItemsEditor.tsx`'s categories filter by the bundled Enquiry's service types (UI-only, not a server rule); new "Copy for Email" clipboard-table button; new read-only `GET /api/quotations/[id]/versions/[versionNumber]/line-items` powers a Version History drill-down. PDF/HTML preview deliberately left unchanged (see stage-12d.md's decision #4).

Before starting a new stage, read `docs/stage-checklists/stage-{N}.md` for each completed stage — what was built, DB models added, decisions/assumptions made and why. Don't re-read the full codebase or re-derive decisions already written down there; open a specific file only to confirm an exact shape (a Prisma field name, a component prop) when it matters.

Models so far: `User`, `Role`, `Branch`, `Session` (login-audit log only, not the real auth session), `FieldPermission` (Stage 0) · `Organization`, `KycDetail`, `ImportBatch`, `ImportRowError` (Stage 1) · `Enquiry`, `EnquiryFreightDetail`, `EnquiryCustomsDetail`, `EnquiryTransportDetail` (Stage 2) · `OrganizationBranch`, `BranchAddress`, `BranchContact`, `BranchAccountManager`, `OrganizationBankAccount`, `CustomerAccountInfo`, `VendorAccountInfo`, `BillType`, `OrganizationBillType`, `UserColumnPreference` (Customer Master v2) · `Quotation`, `QuotationEnquiry`, `QuotationVersion`, `QuotationLineItem` (Stage 3 — Enquiry↔Quotation is many-to-many, see `docs/stage-checklists/stage-3.md`) · `Job`, `ShipperDetail`, `ConsigneeDetail`, `NotifyPartyDetail`, `ContainerDetail` (Stage 4 — a Job comes from a CONVERTED Quotation's per-enquiry `jobSnapshot`, direct create, or bulk import; enums `JobStatus`/`JobOrigin`; `capabilities.ts` gained a `"jobs"` screen; see `docs/stage-checklists/stage-4.md`) · `WorkflowTemplate`, `WorkflowStep`, `JobWorkflowProgress`, `JobAuditLog` (Stage 5 — data-defined step tracks keyed by `(shipmentType, incotermKey)`; templates seeded from `src/lib/workflow/import-tracks.ts`, admin-editable at `/settings/workflow-templates`; `attachWorkflow` in `src/lib/workflow/engine.ts` copies steps onto a Job on Branch Manager approval; per-step actions gated on `WorkflowStep.ownerRole`/`approverRole`, not the coarse `workflowStatus` field perm; `JobAuditLog` is append-only; enum `WorkflowStepStatus`; `capabilities.ts` gained a `"workflowTemplates"` screen; see `docs/stage-checklists/stage-5.md`) · Stage 6 extends the same models, no new tables — `WorkflowStep.isFinal`/`isSkippable` columns, `Job.exportStuffingType` (enum `ExportStuffingType`), 9 Export `WorkflowTemplate` rows (CIF/DDP/DDU × none/Dock/Factory stuffing) seeded from `src/lib/workflow/export-tracks.ts` with stuffing type folded into `incotermKey` as a suffix (`"CIF-DOCK"`) via `templateLookupKeys()` in `src/lib/workflow/types.ts`; new `skip` step action; see `docs/stage-checklists/stage-6.md`. · `DocumentType` (seeded lookup — BillType twin), `Document`, `DocumentVersion`, plus additive `User.organizationId` (Stage 7 — five generated PDF kinds via `@react-pdf/renderer` + uploads; file bytes in `DocumentVersion.bytes` behind `src/lib/pdf/document-storage.ts`; sync 3-attempt retry with HTML-preview fallback; `DRAFT→PENDING_APPROVAL→APPROVED/REJECTED` + `sharedWithCustomer` gate; `capabilities.ts` gained `"documents"`/`"documentTypes"` screens and the first `CUSTOMER` capability; §4.3 nuance in `src/lib/permissions/document-access.ts`; PDF components in `src/lib/pdf/documents/`; per-Job `DocumentsPanel` + global `/documents` + `/settings/document-types`; see `docs/stage-checklists/stage-7.md`). · `PortalAccessLog` (append-only, enum `PortalAccessOutcome`) — Stage 9. No other schema change: `User.organizationId` is now promoted into the NextAuth session (`auth.config.ts` jwt/session callbacks + `src/types/next-auth.d.ts`). Customer-only, read-only, org-scoped `src/app/(portal)/` route group + isolated `src/app/api/portal/*` (only doc-file + quotation-PDF streams), all gated by `src/lib/config/flags.ts` `customerPortalEnabled` (`CUSTOMER_PORTAL_ENABLED` env). `src/lib/portal/{guard,queries}.ts` holds the access + fetch layer; `(dashboard)/layout.tsx` + `authorized()` redirect CUSTOMER↔internal. Admin links a customer login to an Organization via `settings/users` (`src/lib/users/user-write.ts`). See `docs/stage-checklists/stage-9.md`. · **Stage 10**: additive `Job.expectedDeliveryDate`/`Job.actualDeliveryDate` + `@@index([branchId,createdAt])`/`@@index([organizationId,createdAt])` (10b); `Notification` + `NotificationPreference`, enums `NotificationType`/`NotificationDeliveryStatus` (10c — the `Notification` row is both the in-app item and the email-outbox/retry queue); `Session` gains `@@index([userId])`/`@@index([createdAt])` and its `ipAddress`/`userAgent` are now written on login, `JobAuditLog` gains `@@index([actorId])` (10d). No other new tables. Migrations `20260903120000_stage_10b_delivery_dates`, `20260903180000_stage_10c_notifications`, `20260903200000_stage_10d_audit_indexes` — all applied via the Neon serverless-adapter workaround (`[[prisma-migrate-neon-workaround]]`). · **Stage 12a**: `Port`, `EnquiryCommodityLine`, `EnquiryFreightPackage`, enum `DimensionUnit` — see `docs/stage-checklists/stage-12a.md`. Migrations `20260905120000_enquiry_field_rework`, `20260905121500_freight_package_container_type`. **Stage 12c**: `EnquiryFieldConfig` — see `docs/stage-checklists/stage-12c.md`. Migration `20260905130000_enquiry_field_config`. (Stage 12b added no schema.) · **Stage 12d**: no new tables, additive `QuotationLineItem.remarks`/`exchangeRate`/`rateInr` — see `docs/stage-checklists/stage-12d.md`. Migration `20260905140000_quotation_line_item_currency`.

## Environment gotchas (this dev machine)

- Node/npm are installed but not always on PATH in a fresh shell. PowerShell: prefix commands with `$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")`. Bash: call `/c/Program Files/nodejs/node.exe` by full path.
- **Restart the dev server after any Prisma schema change.** `npx prisma generate` rewrites the client on disk, but an already-running `next dev` process keeps the stale one in memory — "Unknown argument" or "Cannot read properties of undefined" on a field/model that clearly exists in schema.prisma is the tell. Kill and restart, don't debug the schema.
- Neon connection string lives in `.env` (gitignored) as `DATABASE_URL`. Never guess or fabricate one — ask the user if it's needed and not already available.
- Test users (all seeded, password `password123`): admin@test.seawave.com, branchmgr@test.seawave.com, doer@test.seawave.com, sales@test.seawave.com, accounts@test.seawave.com, customer@test.seawave.com.

## Conventions
- All schema changes are additive only — never drop/rename existing columns without an explicit migration discussion.
- Prisma 7 has no `datasource.url` in schema.prisma — connections go through a driver adapter. See `src/lib/db/prisma.ts` (the `@prisma/adapter-neon` singleton pattern) before writing any new Prisma-touching script.
- `src/proxy.ts` (Next.js 16 renamed `middleware.ts` → `proxy.ts` — see its file comment if this trips up a future edit) is the request gate, and its matcher excludes `/api/*` entirely — there is **no edge/proxy-level auth on API routes**. Every route handler must call `auth()` and check permissions itself (see any existing route under `src/app/api/` for the pattern).
- Three separate, deliberately-not-unified permission mechanisms — don't conflate them: `src/lib/permissions/access-matrix.ts` (sidebar nav visibility, boolean), `capabilities.ts` (per-action view/create/edit/delete/approve per role per screen), `field-permissions.ts` (DB-backed per-field-group access, for the Job entity's Section 4.3 matrix specifically).
- Soft delete only, structurally — no DELETE verb exists on any API route anywhere in the app. Deactivation is always PATCH `{isActive: false}`.
- Reuse before rebuilding: generic UI primitives live in `src/components/ui/` (Button, Card, Badge, Modal, Input, Select, Textarea, Checkbox, DataTable, StepTracker, FileDropzone, Combobox, ColumnPicker), shared cross-feature components in `src/components/shared/` (CustomerFormModal). Check there before writing something that might already exist.
- Verify with direct API calls per role (a small Node fetch script, not just UI clicking) before calling a stage done — this has caught a real bug in every stage so far (a stale Prisma client, a transaction-timeout from per-row inserts vs. Neon's real network latency, a type-design flaw in a generic component, a React 19 lint rule catching a genuinely fragile pattern). Clean up test/verification data from the DB afterward — check `docs/stage-checklists/stage-2.md`'s cleanup section for the pattern (a small throwaway `tsx` script run and deleted, not committed).
- Do not re-read the full codebase before starting a new stage.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
